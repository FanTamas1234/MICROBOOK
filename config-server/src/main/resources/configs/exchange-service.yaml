spring:
  datasource:
    url: jdbc:postgresql://${EXCHANGEDB_CONTAINER_NAME}:${POSTGRESDB_DOCKER_PORT}/${POSTGRESDB_EXCHANGE_NAME}
    username: ${POSTGRESDB_USER}
    password: ${POSTGRESDB_PASSWORD}
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate.ddlAuto: update
    database: postgresql
    databasePlatform: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrapServers: ${KAFKA_CONTAINER_NAME}:9092
    producer:
      keySerializer: org.apache.kafka.common.serialization.StringSerializer
      valueSerializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: tradeInfoRequest:com.booking.book.exchangeservice.dto.ExchangeTradeRequestDto
    consumer:
      groupId: exchange-group
      autoOffsetReset: earliest
      keyDeserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      valueDeserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        #        spring:
        #          deserializer:#            value:
        #              delegate:#                class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json:
          trusted.packages: '*'
          type.mapping: tradeResponseDto:com.booking.book.exchangeservice.dto.ExchangeTradeResponseDto

server:
  port: ${EXCHANGE_SERVER_PORT}

eureka:
  instance:
    hostname: ${EXCHANGE_SERVICE_CONTAINER_NAME}
  client:
    registerWithEureka: true
    fetchRegistry: true

springdoc:
  api-docs:
    enabled: true
    path: /api/v1/exchange/v3/api-docs
  swagger-ui:
    enabled: true
    disable-swagger-default-url: true
    path: /api/v1/exchange/swagger-ui.html